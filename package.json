{
  "name": "module-sync",
  "version": "0.0.1",
  "description": "Manages building and syncing multiple node modules.",
  "main": "./lib/index.js",
  "typings": "./lib/index.d.ts",
  "scripts": {
    "test": "sh sh/test.sh",
    "tdd": "yarn run test -- --reporter min --watch",
    "lint": "tslint 'src/**/*.ts{,x}' --format verbose",
    "build:typescript": "rm -rf ./lib && tsc",
    "build": "yarn run build:typescript",
    "prepublish": "yarn run lint && yarn test && yarn run build"
  },
  "dependencies": {
    "js-util-log": "^1.0.4"
  },
  "devDependencies": {
    "@types/chai": "^3.5.1",
    "@types/mocha": "^2.2.41",
    "chai": "^3.5.0",
    "js-typescript": "^2.3.1",
    "mocha": "^3.2.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/philcockfield/module-sync"
  },
  "license": "MIT"
}
